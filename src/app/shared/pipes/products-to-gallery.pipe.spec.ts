import { ProductsToGalleryPipe } from './products-to-gallery.pipe';

describe('ProductsToGalleryPipe', () => {
  it('create an instance', () => {
    const pipe = new ProductsToGalleryPipe();
    expect(pipe).toBeTruthy();
  });
});
